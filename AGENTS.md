## Workspace Instructions

- For frontend or UI design/implementation requests in this repository, prefer using the configured `stitch` MCP server first when it is available.
- If `stitch` is unavailable or a request cannot be completed through it, fall back to the normal implementation workflow and mention that fallback briefly in the handoff.
